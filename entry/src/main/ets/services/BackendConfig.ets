// Use 127.0.0.1 for the local emulator.
// For a physical watch, replace with your computer's LAN IP.
// For hosted deployments, use your own trusted wss:// proxy URL.
export const ASR_BACKEND_URL = 'ws://127.0.0.1:8765/listen';
